import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    containerTools: {
        flexDirection: "row",
    },
    botton: {
        backgroundColor: "#092B48",
        padding: 5,
        margin: 10,
        borderRadius: 10
    }
})